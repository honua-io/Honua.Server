# Python Integration Test Dependencies
# Core testing framework
pytest>=7.0.0
pytest-xdist>=3.0.0  # For parallel test execution

# HTTP and API clients
requests>=2.28.0

# STAC (SpatioTemporal Asset Catalog) testing
pystac-client>=0.7.0
pystac>=1.8.0

# Raster data testing (WCS, COG)
rasterio>=1.3.0
numpy>=1.21.0

# OGC Web Services testing (WMS, WFS, WMTS, CSW, WCS)
owslib>=0.29.0

# Image processing and validation
Pillow>=9.0.0

# Geospatial utilities
shapely>=2.0.0

# JSON schema validation (for STAC)
jsonschema>=4.0.0

# Optional: ArcGIS REST API testing
# Uncomment if testing with ArcGIS Python API
# arcgis>=2.0.0

# Optional: GeoPandas for spatial dataframe testing
# Uncomment if testing spatial dataframes
# geopandas>=0.12.0
# pandas>=1.5.0
