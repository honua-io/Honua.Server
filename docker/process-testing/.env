# Process Testing Stack Environment Variables
# This file configures the local testing infrastructure for Honua Process Framework

# Redis Configuration
REDIS_PORT=6379

# Prometheus Configuration
PROMETHEUS_PORT=9090

# Grafana Configuration
GRAFANA_PORT=3000
# IMPORTANT: Change these credentials for production deployments
# Default admin/admin is only for local testing
GRAFANA_USER=admin
GRAFANA_PASSWORD=change-me-in-production

# Loki Configuration
LOKI_PORT=3100

# Tempo Configuration (Distributed Tracing)
TEMPO_PORT=3200
# Note: Tempo also uses 4317/4318 internally but binds to the tempo service
# The OTel collector forwards traces to tempo:4317
TEMPO_OTLP_GRPC_PORT=4317
TEMPO_OTLP_HTTP_PORT=4318
TEMPO_ZIPKIN_PORT=9411
TEMPO_JAEGER_THRIFT_PORT=14268

# OpenTelemetry Collector Configuration
OTLP_GRPC_PORT=4317
OTLP_HTTP_PORT=4318

# Optional: Docker Host IP (for connecting from containers to host)
# DOCKER_HOST_IP=host.docker.internal

# Optional: Enable verbose logging
# COMPOSE_HTTP_TIMEOUT=120
# DOCKER_CLIENT_TIMEOUT=120
