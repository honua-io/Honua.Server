{
  "name": "honua-secret-rotation",
  "version": "1.0.0",
  "description": "Automated secret rotation for HonuaIO",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "deploy": "zip -r function.zip . && aws lambda update-function-code --function-name honua-secret-rotation --zip-file fileb://function.zip"
  },
  "dependencies": {
    "aws-sdk": "^2.1400.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  },
  "keywords": [
    "aws",
    "lambda",
    "secrets",
    "rotation",
    "security"
  ],
  "author": "HonuaIO",
  "license": "MIT"
}
