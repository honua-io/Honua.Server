apiVersion: v1
kind: Secret
metadata:
  name: honua-database-secret
  namespace: honua
  labels:
    app.kubernetes.io/name: honua
    app.kubernetes.io/component: database
    environment: production
type: Opaque
stringData:
  # TEMPLATE: Replace these values with actual credentials
  # In production, use external secret management (e.g., Sealed Secrets, External Secrets Operator, Vault)
  username: "honua_user"
  password: "CHANGE_ME_STRONG_PASSWORD_HERE"
  connection-string: "Host=postgis-service;Port=5432;Database=honua;Username=honua_user;Password=CHANGE_ME_STRONG_PASSWORD_HERE;MinPoolSize=2;MaxPoolSize=50;ConnectionLifetime=600;SSL Mode=Require"
---
apiVersion: v1
kind: Secret
metadata:
  name: honua-redis-secret
  namespace: honua
  labels:
    app.kubernetes.io/name: honua
    app.kubernetes.io/component: cache
    environment: production
type: Opaque
stringData:
  # TEMPLATE: Replace with actual Redis password
  password: "CHANGE_ME_REDIS_PASSWORD"
---
apiVersion: v1
kind: Secret
metadata:
  name: honua-jwt-secret
  namespace: honua
  labels:
    app.kubernetes.io/name: honua
    app.kubernetes.io/component: auth
    environment: production
type: Opaque
stringData:
  # TEMPLATE: Generate a strong signing key (minimum 256 bits / 32 bytes)
  # Example: openssl rand -base64 32
  signing-key: "CHANGE_ME_GENERATE_STRONG_KEY_BASE64"
---
apiVersion: v1
kind: Secret
metadata:
  name: honua-storage-secret
  namespace: honua
  labels:
    app.kubernetes.io/name: honua
    app.kubernetes.io/component: storage
    environment: production
type: Opaque
stringData:
  # TEMPLATE: Cloud storage credentials (AWS S3 / Azure Blob / GCS)
  # For AWS
  aws-access-key-id: "CHANGE_ME_AWS_ACCESS_KEY"
  aws-secret-access-key: "CHANGE_ME_AWS_SECRET_KEY"
  # For Azure
  azure-storage-account: "CHANGE_ME_STORAGE_ACCOUNT"
  azure-storage-key: "CHANGE_ME_STORAGE_KEY"
  # For GCS
  gcs-credentials-json: |
    {
      "type": "service_account",
      "project_id": "CHANGE_ME"
    }
